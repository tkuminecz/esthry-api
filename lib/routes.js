/* global define*/
define([], function() {
	'use strict';

	/*
	 
	   /asset/
	   	GET    - returns a list of assets in the collection
		POST   - creates a new asset in the collection
		DELETE - deletes the entire collection
		PUT    - replace the collection

	   /asset/:id
	   	GET    - returns the particular asset
		POST   - n/a
		DELETE - delete an asset
		PUT    - replace the assset

	 */
});
